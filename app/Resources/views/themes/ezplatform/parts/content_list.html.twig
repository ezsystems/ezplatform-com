{% set extraParams = extraParams is defined ? extraParams %}

{% if items.searchHits|length %}
    {% for hit in items.searchHits %}
        {{ render(controller('ez_content:viewAction', {
            'locationId': hit.valueObject.contentInfo.mainLocationId,
            'viewType': viewType,
            'params': {
                'extra': extraParams
            }
        })) }}
    {% endfor %}
{% endif %}
